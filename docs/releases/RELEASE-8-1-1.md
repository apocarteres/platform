---
id: RELEASE-8-1-1
type: release
status: released
scope: release
authority: supporting
opened-on: 2026-09-24
released-on: 2026-09-24
commit: d2c2880c4624ef0e5b01b0c4cb61e3c29f388275
---

# Выпуск 8.1.1

Правила выпуска — `REQ-RELEASE` в поставке пакета правил. [Каталог](INDEX.md)

## Цель

Выпустить состояние сервиса, накопленное после предыдущего выпуска, и исполнить обязательства ядра, попавшие в этот выпуск.

## Состав

| Задача | Причина включения |
|---|---|
| [CORE-OPS-091](../tickets/closed/CORE-OPS-091-hook-lost-docs-check.md) | Закрыта в этом выпуске, приоритет P1 |

## Критерии выхода

- [x] Набор `verify` пройден на выпускаемом коммите — расписка 2026-09-24T20:02:31.025Z, наборы: check, verify, прогон `mise run verify-set`
- [x] Тег выпуска создан на проверенном коммите — `v8.1.1`
- [x] Обязательства ядра этого выпуска закрыты или перенесены записью с причиной — ядро не объявляет обязательств самому себе
- [x] Завершающий шаг выполнен — публикация артефактов ядра: mise run install-local

## Не входит

Задачи, не закрытые к моменту закрытия выпуска: они попадут в состав следующего по факту закрытия.

## Проверка видит больше

- Хук `pre-push` ядра после расписок прогоняет `docs-check`: дерево, чьи документы проверка не принимает, в `main` не уходит (`REQ-QUALITY-004`).

## Миграция

Правок не требуется. Принятая идея с полем `ticket` теперь проходит проверку документов; у задачи поле `ticket` по-прежнему отвергается.

## Результат

Выпущено с коммита `d2c2880c4624ef0e5b01b0c4cb61e3c29f388275`, тег `v8.1.1`.

Расписка о проверках получена 2026-09-24T20:02:31.025Z; выполненные наборы: check, verify.

Прогон наблюдён командой `mise run verify-set` с кодом возврата 0.

Развёртывание выполняется этим тегом: REQ-RELEASE-016.

## Цена обновления

Несовместимого нет: требования и объявленное поведение не менялись, но проверка видит больше — см. ниже.

Проверка видит больше (1) — нарушение, прежде невидимое, может найтись:
- Хук `pre-push` ядра после расписок прогоняет `docs-check`: дерево, чьи документы проверка не принимает, в `main` не уходит (`REQ-QUALITY-004`).

---
id: CORE-QUAL-016
type: ticket
status: done
scope: rust, quality
authority: supporting
priority: P1
release: RELEASE-1-24-0
related: REQ-CODE-COMMENTS, REQ-QUALITY
---

# Правило комментариев воевало бы с документацией Rust

## Проблема

`REQ-CODE-COMMENTS` допускает комментарий только как ссылку на документ и
запрещает пояснительный текст. В Rust-код правило не смотрит: `.rs` нет в
перечне расширений.

Просто дописать расширение нельзя. В Rust `///` и `//!` — не комментарии, а
**документация элемента**: это контракт, который читает потребитель крейта и
который язык умеет извлекать. Разбор ядра этой разницы не знает.

## Чем подтверждено

Проба на обычном для Rust файле: разбор вернул три «комментария», из них два —
документация, причём с обрезанным первым символом.

```
"! Модуль расчёта."
"/ Возвращает цену с налогом. / Налог берётся из настройки."
"здесь мы прибавляем налог, потому что так надо"
```

Пояснительный комментарий здесь ровно один — третий. Дописав `.rs` в перечень,
ядро объявило бы нарушением каждую строку документации в каждом файле, то есть
воевало бы с языком, который её требует.

## Что сделано

Разбор комментариев научен различать документацию элемента и обычный
комментарий, и различение включается по языку: для `.rs` документацией считаются
`///`, `//!`, `/** */` и `/*! */`. Правило применяется только к остальным.

Поведение Java не тронуто: звёздочный комментарий там документацией не
объявляется, и это закреплено отдельной проверкой — иначе разбор двух языков
незаметно смешался бы.

`.rs` добавлен в общий перечень расширений, поэтому потребовалось развести и три
соседних правила: часы (`CORE-QUAL-014` отдал их clippy), суффикс `-er`
(`CORE-QUAL-018`) и денежные величины (`CORE-QUAL-017`).

## Чем закреплено

Три проверки: файл с документацией проходит, пояснение внутри тела ловится,
Java не изменилась. Различающая сила подтверждена двумя пробами — снятием
различения документации и удалением `.rs` из перечня расширений; каждая уронила
свои проверки.

## Откуда пришла задача

Разбор `CORE-QUAL-012`: из пяти правил, читающих код, Rust видит одно.

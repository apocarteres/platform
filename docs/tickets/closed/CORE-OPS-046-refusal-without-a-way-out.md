---
id: CORE-OPS-046
type: ticket
status: done
scope: tooling
authority: supporting
priority: P1
release: RELEASE-1-19-0
related: REQ-RELEASE, REQ-PROJECT-PROCESS
---

# Отказ коммиту без задачи не называет выхода

## Проблема

Отказ закрытия «Коммит ... не называет задачу» сообщает нарушение и молчит о
том, как его исправить. Выходов у исполнителя три — назвать задачу, поставить
признак цикла, пометить коммит меткой отмены, — и ни один из них в сообщении не
назван.

`REQ-RELEASE-039` требует обратного и объясняет, чем платят за молчание: отказ,
называющий нарушение и умалчивающий о выходе, оставляет исполнителя перед
правилом без законного действия и подталкивает к обходу настройки, ради которой
правило введено. Для второго отказа — о задаче вне состава — выход назван. Для
этого нет.

## Чем подтверждено

Заявка потребителя [issue #14](https://github.com/apocarteres/platform/issues/14),
ядро 1.18.0, задача потребителя `ZAVPN-OPS-048`.

Потребитель заключил, что признака цикла не существует, и перебрал четыре
заголовка коммита, ни один из которых не прошёл. Признак существует и объявлен в
`REQ-PROJECT-PROCESS-023` — доставляемом документе: это строка в теле коммита
`Release-cycle: <идентификатор выпуска>`. Потребитель пробовал только заголовки.
Проверено на эталонном потребителе:

```
ПРОХОД «Record the composition of release 2026.09.11» (+ строка признака)
ОТКАЗ  «Rebuild the release index after closing»      (без неё)
```

То есть сама проверка исправна, а заявка появилась потому, что отказ не сказал
того, что знал.

## Что сделано

Решение владельца 2026-09-13: `REQ-RELEASE-039` распространено со своего одного
отказа на все отказы контура. Отказ либо называет действие, которым нарушение
снимается, либо прямо объявляет, что действия нет.

Отказ коммиту, не называющему задачу, перечисляет все три выхода и форму
каждого — включая то, что признак цикла ставится строкой в тело, а заголовок для
него не годится. Идентификатор выпуска подставляется настоящий, чтобы строку
можно было скопировать.

Разбор остальных отказов тем же мерилом нашёл ещё шесть молчавших, и нашла их
проверка, а не чтение: нет расписки, расписка без признака прогона, просроченное
обязательство, занятый тег, незавершённая задача состава на завершающем шаге,
задача, уже отнесённая к другому выпуску, — и, отдельно, «Состав пуст» и
«Рабочее дерево не чисто». Каждому назван выход.

Закреплено двумя проверками: первая читает три выхода и форму признака цикла,
вторая проходит по всем отказам закрытия и требует выхода от каждого. Вторая
поймала два отказа уже после того, как первая была написана. Различающая сила
проверена возвратом прежнего сообщения — проверка отказала.

## Откуда пришла задача

Заявка потребителя [issue #14](https://github.com/apocarteres/platform/issues/14).

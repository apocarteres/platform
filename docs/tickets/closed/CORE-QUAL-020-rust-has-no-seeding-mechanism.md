---
id: CORE-QUAL-020
type: ticket
status: done
scope: rust, quality
authority: supporting
priority: P1
release: RELEASE-1-27-0
related: REQ-QUALITY
---

# У Rust нет средства засева, хотя правило его разрешает

## Проблема

`REQ-QUALITY-007` разрешает засеять существующие находки ограничителем и с
1.20.0 знает вторую единицу — множество объявленных послаблений. Для Java и
TypeScript ядро даёт средство засева файлом. Для Rust средства нет: у clippy
ограничителя не существует, а ядро своего не предложило.

Потребитель остаётся перед выбором: одномоментно править существующий код либо
отойти от поставленной настройки. Ни то ни другое не входило в замысел.

## Чем подтверждено

Заявка потребителя [issue #21](https://github.com/apocarteres/platform/issues/21).
Порог числа аргументов, приезжающий поставкой, дал на агенте потребителя
**тринадцать находок, двенадцать из которых к часам отношения не имеют**:
подписи были такими до переноса настройки. Потребитель порог не оспаривает и
прямо пишет, что он поставлен не зря.

## Что сделано

Правило `rust-exemptions` собирает объявленные в коде послабления clippy и ведёт
их ограничителем единицей множества, введённой для этого в `CORE-QUAL-008`.
Множество засеивается один раз, новое послабление без подъёма роняет проверку,
снятое видно улучшением.

Так у Rust появилось то же средство, что у Java и TypeScript, и работа идёт
снятием послаблений, а не правкой порога.

Разбор читает объявления, а не текст: послабление, упомянутое в комментарии или
в строковом литерале, не считается.

## Чем закреплено

Четыре проверки, из них одна стоит отдельно: **счёт этой работы не выражает.**
Подмена одного послабления другим по счёту незаметна — было два, стало два, — а
по множеству видна. Это довод за выбранную единицу, выраженный проверкой.

Сквозная проба подтвердила весь ход: засев записывает перечень, новое
послабление получает отказ с названием lint, снятие видно улучшением.

## Откуда пришла задача

Заявка потребителя [issue #21](https://github.com/apocarteres/platform/issues/21),
последний раздел.

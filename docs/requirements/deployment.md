---
id: REQ-DEPLOYMENT
type: requirement
status: active
scope: deployment, build
authority: normative
clause-id-prefix: REQ-DEPLOYMENT
related: REQ-BUILD, REQ-QUALITY, REQ-COMPATIBILITY
---

# Развёртывание

[Карта требований](../REQUIREMENTS.md)

Документ нормативен для платформы и для всех проектов, подключивших пакет `@apocarteres/project-conventions`. Он не копируется в репозитории потребителей: пакет доставляет этот файл вместе с собой.

**Область действия: развёртывание любого сервиса.** Требования не зависят от языка и от состава компонентов.

**Уровень требования: директива**, кроме положений, объявляющих иное первой строкой. Умолчание названо здесь, чтобы уровень не приходилось выводить из тона: положение без объявленного уровня — директива.

## Единица развёртывания

1. <a id="REQ-DEPLOYMENT-001"></a> **REQ-DEPLOYMENT-001** — Развёртывается коммит, а не рабочее дерево и не собранный кем-то артефакт. Машина развёртывания получает коммит из репозитория, переключается на него и собирает сама теми же задачами, что разработчик.
2. <a id="REQ-DEPLOYMENT-002"></a> **REQ-DEPLOYMENT-002** — Развёртывание проверяет, что получен именно затребованный коммит, и останавливается при расхождении. Собранная ревизия записывается манифестом рядом с артефактами: без него нельзя ответить, что именно работает на стенде.
3. <a id="REQ-DEPLOYMENT-003"></a> **REQ-DEPLOYMENT-003** — Развёртывание не собирает артефакты из состояния, отличного от коммита: рабочее дерево на машине развёртывания приводится к нему принудительно, кроме каталогов кеша сборки.

## Порядок

4. <a id="REQ-DEPLOYMENT-004"></a> **REQ-DEPLOYMENT-004** — Перед сборкой выполняется проверка предпосылок: наличие инструментов закреплённых версий, настроек окружения и хранилищ. Отсутствие предпосылки останавливает развёртывание до изменений на стенде, а не посередине.
5. <a id="REQ-DEPLOYMENT-005"></a> **REQ-DEPLOYMENT-005** — Резервная копия изменяемых данных снимается до установки новых артефактов.
6. <a id="REQ-DEPLOYMENT-006"></a> **REQ-DEPLOYMENT-006** — После установки выполняется проверка работоспособности сервиса. Развёртывание считается неуспешным, пока проверка не прошла, даже если установка артефактов завершилась без ошибок.
7. <a id="REQ-DEPLOYMENT-007"></a> **REQ-DEPLOYMENT-007** — Операции подготовки данных — создание администратора, миграции хранилищ, публикация ревизий компонентов — выполняются явными командами отдельными шагами развёртывания. Обычный старт сервиса их не выполняет и от них не зависит.
8. <a id="REQ-DEPLOYMENT-008"></a> **REQ-DEPLOYMENT-008** — Шаг развёртывания идемпотентен: повторный запуск на том же коммите не меняет результат и не портит данные. Шаг, который нельзя повторить, оформляется отдельной командой с явным подтверждением.

## Отношение к проверкам

9. <a id="REQ-DEPLOYMENT-009"></a> **REQ-DEPLOYMENT-009** — Развёртывается коммит, на котором прошёл `verify`. Развёртывание само проверки не заменяет и не повторяет: его задача — собрать и установить, а не выяснять качество.
10. <a id="REQ-DEPLOYMENT-010"></a> **REQ-DEPLOYMENT-010** — Развёртывание выполняется задачей менеджера окружения или скриптом репозитория, а не набором команд из инструкции. Ручные шаги, которые нельзя выразить командой, перечисляются в инструкции с указанием, почему они ручные.

## Запуск артефакта

11. <a id="REQ-DEPLOYMENT-011"></a> **REQ-DEPLOYMENT-011** — Ядро собирает исполняемый артефакт сервиса, поэтому ядро же называет, чем он запускается: перечень ключей запуска — часть поставки, а не догадка потребителя. Умолчание здесь стоит столько же, сколько умолчание о наборе статического разбора (`REQ-QUALITY-002`): каждый потребитель выводит ключи заново и выводит по-разному, а разница обнаруживается измерением на производственной среде, если обнаруживается вообще. Ключи объявляются в поставке вместе с артефактом; отказ от ключа — тоже положение и записывается с причиной, а не оставляется молчанием. Решение владельца от 2026-09-17 по заявке потребителя.

12. <a id="REQ-DEPLOYMENT-012"></a> **REQ-DEPLOYMENT-012** — Сервис запускается с архивом классов: `-XX:+AutoCreateSharedArchive -XX:SharedArchiveFile=<путь>`. Первый запуск после установки записывает архив, последующие перезапуски им пользуются. Архив привязан к составу классов артефакта, поэтому шаг развёртывания **удаляет файл архива при установке нового артефакта**. Без удаления архив остаётся несовпадающим навсегда: он не пересоздаётся сам, при каждом запуске сообщает о расхождении в журнал виртуальной машины и не применяется, а сервис при этом поднимается как ни в чём не бывало. Проверено на JDK 25: после смены артефакта расхождение повторяется из запуска в запуск, после удаления файла — исчезает с первого. Это и есть та самая незаметная ошибка, ради которой положение записано: журнал читают не всегда, а «работает» выглядит одинаково с кешем и без. Решение владельца от 2026-09-17 по заявке потребителя.

13. <a id="REQ-DEPLOYMENT-013"></a> **REQ-DEPLOYMENT-013** — **Уровень: рекомендация.** Кеш AOT (JEP 483, 514, 515; доступен с JDK 24) сверх архива классов сокращает прогрев компилятора после установки. Ядро его не предписывает: запись кеша требует пробного прогона настоящей нагрузки при сборке, то есть отдельной машинерии, а выигрыш зависит от того, как часто сервис разворачивают. Измерение потребителя на производственной среде: 189 с процессорного времени на компиляцию — четырнадцать процентов всего, что процесс потратил за три с половиной часа, — и почти всё в первые семь минут, при нуле инвалидаций. Существенно здесь не процессорное время: узел его не замечает. Существенно то, что **первые минуты после установки сервис отвечает медленнее**, потому что горячий путь ещё не скомпилирован, — то есть ровно тогда, когда за ним смотрят после изменения. Проект, разворачивающий сервис несколько раз в неделю, эту рекомендацию исполняет; проект, разворачивающий раз в месяц, вправе её не исполнять. Кеш привязан к сборке JDK так же, как архив классов к составу классов: расхождение версии между сборкой и запуском означает, что кеш не применён, и обнаруживается это только проверкой. Решение владельца от 2026-09-17 по заявке потребителя.

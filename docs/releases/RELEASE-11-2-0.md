---
id: RELEASE-11-2-0
type: release
status: released
scope: release
authority: supporting
opened-on: 2026-09-25
released-on: 2026-09-25
commit: 5539fb49e6a29062c0d755c076e0153f4f244cfe
---

# Выпуск 11.2.0

Правила выпуска — `REQ-RELEASE` в поставке пакета правил. [Каталог](INDEX.md)

## Цель

Выпустить состояние сервиса, накопленное после предыдущего выпуска, и исполнить обязательства ядра, попавшие в этот выпуск.

## Состав

| Задача | Причина включения |
|---|---|
| [CORE-OPS-097](../tickets/closed/CORE-OPS-097-stand-rate-limit-scale.md) | Закрыта в этом выпуске, приоритет P2 |
| [CORE-QUAL-029](../tickets/closed/CORE-QUAL-029-raw-fetch-without-api-version.md) | Закрыта в этом выпуске, приоритет P1 |

## Критерии выхода

- [x] Набор `verify` пройден на выпускаемом коммите — расписка 2026-09-25T08:58:09.455Z, наборы: check, verify, прогон `mise run verify-set`
- [x] Тег выпуска создан на проверенном коммите — `v11.2.0`
- [x] Обязательства ядра этого выпуска закрыты или перенесены записью с причиной — ядро не объявляет обязательств самому себе
- [x] Завершающий шаг выполнен — публикация артефактов ядра: mise run install-local

## Не входит

Задачи, не закрытые к моменту закрытия выпуска: они попадут в состав следующего по факту закрытия.

## Проверка видит больше

- Правило `client-update` (`REQ-CLIENT-UPDATE-011`) находит в исходниках приложения, кроме тестов, голый `fetch('/…')` и `XMLHttpRequest.open(…, '/…')` к своему API: такой запрос уходит без `X-Api-Version` и получает `426` после раската.

## Миграция

1. Голые вызовы к своему API заменить на `apiFetch()` из `@apocarteres/app-update`: функцию получают в контексте внедрения (`private readonly fetch = apiFetch();`) и зовут как `fetch`. Заголовок версии, свои заголовки и показ «клиент устарел» она берёт на себя; явный заголовок версии в таких местах можно убрать.
2. Стенд сценариев поднимает пределы частоты настройкой `platform.rate-limit.scale` (от 1 до 1000) вместо сброса ключей Redis. С профилем Spring `production` множитель больше 1 не стартует (`REQ-AUTH-027`).

## Результат

Выпущено с коммита `5539fb49e6a29062c0d755c076e0153f4f244cfe`, тег `v11.2.0`.

Расписка о проверках получена 2026-09-25T08:58:09.455Z; выполненные наборы: check, verify.

Прогон наблюдён командой `mise run verify-set` с кодом возврата 0.

Развёртывание выполняется этим тегом: REQ-RELEASE-016.

## Цена обновления

Несовместимого нет: требования и объявленное поведение не менялись, но проверка видит больше — см. ниже.

Проверка видит больше (1) — нарушение, прежде невидимое, может найтись:
- Правило `client-update` (`REQ-CLIENT-UPDATE-011`) находит в исходниках приложения, кроме тестов, голый `fetch('/…')` и `XMLHttpRequest.open(…, '/…')` к своему API: такой запрос уходит без `X-Api-Version` и получает `426` после раската.

Добавлено (1):
- положений добавлено: 2
